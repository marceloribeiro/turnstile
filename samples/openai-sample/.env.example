# Your OpenAI API key. Turnstile passes it straight through and never stores it.
OPENAI_API_KEY=

# Turnstile data plane base URL (the sample appends /v1).
TURNSTILE_BASE=http://localhost:8080

# Optional overrides.
MODEL=gpt-4o-mini
# TURNSTILE_SESSION=openai-sample
